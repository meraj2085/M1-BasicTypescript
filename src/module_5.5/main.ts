// import { add as add1, subtract } from './module';
// import * as Methods from './module';
import jekonoNam from './module';
const add = (param1: number, param2: number, param3: number): number => {
     return param1 + param2 + param3;
}
// const res = add1(1, 2)
// const res = Methods.divide(1, 2)
const res = jekonoNam.divide(1, 2)